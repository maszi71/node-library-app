# Environment variables offer information on the process operating environment
# (producton, development, build pipeline, and so on). 
# Environment variables in Node are used to store sensitive data such as passwords,
#  API credentials, and other information that should not be written directly in code.
#  Environment variables must be used to configure any variables or
#  configuration details that may differ between environments.
PORT=4000
DB_URL = mongodb://localhost:27017
DB_NAME = library